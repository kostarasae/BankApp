# Database settings
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:bankapp}?useSSL=true&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER:cf9}
spring.datasource.password=${MYSQL_PASSWORD:1}

spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# disable first time to generate create tables
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

app.security.secret-key=kJlhehcq7kXmCk02Gng4vG6LE+9nzyFnPeXnHdpc3PVw9u8fLvA1CpznI1lVLvV5
app.security.jwt-expiration=10800000

# make sure to use join fetch
spring.jpa.open-in-view=false

allowed.origins=http://localhost:5173,http://localhost:3000